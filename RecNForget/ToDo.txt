add Signature!?!

playback/recordingservice events mess up taskbar state
try migrate old settings from appsettingsservice to new userconfigurationservice settings on start
getting updates should be cancellable
renew/ improve? copyright helper/ content helpers
better place for lame mp3 dlls?
viewmodel testing

stacked buttons ontop of each other (StringSettingsButton, others?)
extra button in StringSettingsButton is useless now
dialogs, topmost, viewposition, dragging and exiting (also via esc)

rethink designtime services/viewmodels?

IsEnabledCondition1/IsEnabledCondition2 should be not needed when using viewmodels

fix stylechanges for playpause- and togglerecord-buttons (with glyph icons?)

Commands on usercontrols should be properly named "ActionNameRelayCommand" :D
how best to ensure settings/about/ other windows open on top of main window if main window is set to always on top? (also affects other dialogs opened from another window)

menu item icons
theme selector

windowpositions for subwindows (context menu)

non global hotkeys disabled for now

notificationmanager via di

using IRecipient-interfaces for messages between viewmodels instead of directly accessing viewmodels from one another

editorconfig/ codecleanup rules